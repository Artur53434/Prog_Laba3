#include <conio.h>
#include <stdio.h>
#include <Windows.h>
#include <stdbool.h>

#define SIZE_MENU 7

void Menu(const char** list_menu, int point);

int main() {
  bool is_exit = false;

  // Подключение русского языка
  SetConsoleOutputCP(65001);
  SetConsoleCP(65001);

  while (isExit == false) {

  }

  return 0;
}
